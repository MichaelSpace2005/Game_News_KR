<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/edit_text_background"
    android:layout_marginVertical="12dp">

    <TextView
        android:id="@+id/newsTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Адская гриндилка!"
        android:textSize="20sp"
        style="@style/TitleText"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginVertical="12dp"
        android:layout_marginHorizontal="24dp"
        app:layout_constraintEnd_toStartOf="@+id/newsEyeBtn"
        android:textAlignment="textStart"/>

    <TextView
        android:id="@+id/newsSubtitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        style="@style/SubTitleText"
        android:textSize="14sp"
        android:text="геймер из рязани после катки не узнал"
        app:layout_constraintTop_toBottomOf="@id/newsTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:maxLines="2"
        android:lines="2"
        android:ellipsize="end"
        android:layout_marginHorizontal="24dp"
        android:layout_marginVertical="12dp"/>

    <ImageView
        android:id="@+id/newsEyeBtn"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/eye_open"
        app:tint="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/newsTitle"
        app:layout_constraintTop_toTopOf="@+id/newsTitle"
        android:layout_marginEnd="24dp"/>


</androidx.constraintlayout.widget.ConstraintLayout>